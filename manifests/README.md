# Dataset manifests

Store small, reviewable descriptions of external datasets and preprocessing versions here. Do not store dataset payloads.

A manifest should include source identity, sample counts, split version, EEG channels, sampling rate, frequency-band filters, time windows, normalization, image preprocessing, image-EEG mapping, and optional checksums.
