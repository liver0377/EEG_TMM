# Scripts

Command-line entry points belong here. Keep reusable logic in `src/eeg_tmm/`. Scripts must accept paths and experiment settings through arguments or configuration rather than hardcoded absolute paths.
