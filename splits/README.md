# Dataset splits

Store deterministic train/validation/test sample identifiers here. Split files must never depend on directory iteration order. Include a version and document the mapping from each identifier to its image and EEG sample.
